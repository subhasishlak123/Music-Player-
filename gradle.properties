# Enable AndroidX to support modern libraries like Media3 and Compose
android.useAndroidX=true
# Automatically convert old libraries to AndroidX if needed
android.enableJetifier=true
# Speed up the build on GitHub Actions
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
